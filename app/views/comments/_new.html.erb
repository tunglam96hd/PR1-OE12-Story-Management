<%= form_for :comment, url: comments_path do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: t(".compose_comment") %>
  </div>
  <%= f.submit t(".commnent"), class: "btn btn-primary" %>
  <%= f.hidden_field :story_id, value: @story.id %>
<% end %>
