<%= form_for @comment, remote:true do |f| %>
  <div class="field">
    <%= f.text_area :content, placeholder: t(".edit_commnent") %>
  </div>
  <%= f.submit t(".edit"), class: "btn btn-primary" %>
<% end %>
